## Lab 7 - User ID controlled by request parameter 
**End Goal** - Obtain the API key for the user carlos and submit it as the solution.  
  
---
### Analysis:  
Given:  
-> Your credentials `wiener:peter`  
  
- I logged in using my credentials.  
  
- The page shows me my username `wiener` and my API key `d9obahq5OTlElzhGGVqa27SjubUF3V2B` on the screen. And the URL is like - `/my-account?id=wiener`.  
  
- Now i just changed the URL to `/my-account?id=carlos` and now i can see my username as `carlos` and my API key `YL20ga54s1LZAT4OV5KxnoPVBjyMUs79`.  
  
- I just copied that API key for carlos and submitted it. very simple!